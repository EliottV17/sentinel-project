# =============================================================================
# Sentinel - Docker Compose Environment Variables
# =============================================================================
# Secret key for sentinel-api JWT signing
SECRET_KEY=change-me-to-a-random-secret

# Allowed CORS origins for sentinel-api (separated by commas)
CORS_ORIGINS=http://localhost:5173
